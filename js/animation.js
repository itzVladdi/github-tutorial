const buttonSwitch = document.querySelector('.btn');
const textOne = document.querySelector('one');
const textTwo = document.querySelector('two')

buttonSwitch.addEventListener("click", () => {
    buttonSwitch.classList.toggle()
} )